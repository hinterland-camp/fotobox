# Shared secret the photo booth sends when uploading a photo.
# Without it the server refuses every upload, so anyone who knows the domain
# cannot post images. Generate one with:
#   openssl rand -base64 32
# The same value goes into the booth's settings panel under "Upload Token".
NUXT_UPLOAD_TOKEN=
